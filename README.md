
# nvim config

These are the dotfiles for my neovim config.

## Usage

To install, run the following:

```bash
git clone https://github.com/kashsuks/nvim .config/nvim
```

This will install my dotfiles under `.config/nvim`
